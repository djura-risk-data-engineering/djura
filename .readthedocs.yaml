version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  jobs:
    pre_install:
      - pip install "poetry-core>=2.0.0,<3.0.0" "poetry-dynamic-versioning>=1.4.0,<2.0.0"

sphinx:
  configuration: docs/source/conf.py
  fail_on_warning: true

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        # autodoc imports every submodule, so all application dependencies
        # must be present or the import failures become warnings, which
        # fail_on_warning turns into a failed build
        - all
        - plot
    - requirements: docs/requirements.txt
