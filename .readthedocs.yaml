version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  commands:
    - pip install poetry
    - poetry install --with docs --without dev --no-interaction
    - python -m sphinx docs/source $READTHEDOCS_OUTPUT/html

sphinx:
  configuration: docs/source/conf.py
  fail_on_warning: true
